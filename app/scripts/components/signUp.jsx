var React = require('react');
var setupParse = require('../parseUtilities.js');

var SignUpContainer = React.createClass({
  render: function(){
    return(
      <h1>Sign Up Container</h1>
    );
  }
});

module.exports = {
  SignUpContainer: SignUpContainer
};
